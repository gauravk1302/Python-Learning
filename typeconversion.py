#example of type conversion 
#in this conversion will done by complier itself (automatically)
a,b=3,4.5
print(a+b)# here the first variable is of int type and second is of float so the compiler will 
# automatically a jo int type hai usse float le lega as 3.0 aur phir operation perform karega 